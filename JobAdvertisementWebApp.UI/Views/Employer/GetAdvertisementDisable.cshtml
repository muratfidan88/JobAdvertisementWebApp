@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model List<AdvertisementListDto>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <title></title>
</head>
<body>
    @{ if (Model.Count == 0)
        {
            <div>
                <h4>Şirkete ait yayından kaldırılmış ilan bulunmamaktadır.</h4>
            </div>
            <div class="mb-3 text-end">
                <a class="btn btn-primary" asp-action="GetAdvertisement" asp-route-id="@ViewBag.CompanyId" role="button">Yayındaki İlanları Görüntüle!</a>
            </div>
        }
        else
        {
            int i = 0;
            foreach (var item in Model)
            {
                <div class="accordion" id="accordionExample">
                    <div class="accordion-item">
                        <h2 class="accordion-header">
                            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse_@i" aria-expanded="false" aria-controls="collapse_@i">
                                <strong>@item.Title</strong>
                            </button>
                        </h2>
                        <div id="collapse_@i" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
                            <div class="accordion-body">
                                <div>
                                    <strong>İlan Açıklaması : </strong>@item.Defination
                                </div>
                                <div class="mb-3 mt-3 text-end">
                                    <a class="btn btn-primary" asp-action="EnableAdvertisement" asp-route-id="@item.Id" role="button">İlanı Yayına Aç!</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                i++;
            }
        }
    }

</body>
</html>